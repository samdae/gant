# Proposal v6: 회고분석 배점 + 포트폴리오 모드

> **상태**: 미구현 — v5 안정화 및 데이터 축적 후 착수  
> **선행 조건**: v5 (RAG 검색 파이프라인 개편, RAG Validator) 운영 안정화

---

## 1. 목적

본 제안은 두 가지를 정의한다:

1. **회고분석 배점** (우선) — 회고분석 에이전트가 분석 정확도와 RAG 기여도를 0~100으로 수치화. 대시보드에서 시스템 성능을 한눈에 파악
2. **포트폴리오 모드** (향후) — 공유 가상 자금 풀에서 종목 간 배분을 결정하는 실전 시뮬레이션 모드

---

## 2. 회고분석 배점 [결정됨]

### 2-1. 배경

현재 회고분석은 텍스트 분석 결과(`analysis_content`)만 저장한다. 승률(wins/losses)은 "이겼나 졌나"만 보지만, 회고분석 배점은 **과정의 타당성**을 수치화한다:

- 분석이 맞았는데 운 나빠서 진 것 → 분석 정확도 높음, 수익률 마이너스
- 분석이 틀렸는데 운 좋게 이긴 것 → 분석 정확도 낮음, 수익률 플러스

회고분석 에이전트는 전체 사이클 리포트 + 매매 이력 + RAG 문서를 다 보고 나서 판정하므로 단순 승률보다 정밀하다.

### 2-2. 스키마 변경

`retrospective_analyses` 테이블에 컬럼 추가:

```sql
ALTER TABLE retrospective_analyses ADD COLUMN analysis_accuracy INTEGER;
ALTER TABLE retrospective_analyses ADD COLUMN rag_contribution INTEGER;
```

| 컬럼 | 타입 | 범위 | 설명 |
|------|------|------|------|
| `analysis_accuracy` | INTEGER | 0~100 | 12에이전트 분석의 실제 시장 움직임 대비 정확도 |
| `rag_contribution` | INTEGER | 0~100, NULL | RAG 경험의 PA 판단 기여도. RAG 미사용 시 NULL |

### 2-3. 배점 기준

#### analysis_accuracy (0~100)

12에이전트 파이프라인의 분석이 실제 시장 움직임과 얼마나 일치했는가.

| 점수 | 기준 |
|------|------|
| 90~100 | 방향, 타이밍, 근거 모두 정확. 분석 논리가 시장 결과와 완전히 일치 |
| 70~89 | 방향은 맞았으나 타이밍 또는 근거에 부분 오류. 핵심 판단은 유효 |
| 50~69 | 방향은 맞았으나 근거가 부실하거나 우연히 맞음. 재현 가능성 낮음 |
| 30~49 | 방향이 틀렸으나 일부 분석(섹터, 매크로 등)은 유효 |
| 0~29 | 전반적으로 부정확. 시장 상황을 오독 |

#### rag_contribution (0~100, RAG 미사용 시 null)

RAG로 주입된 과거 경험이 PA 판단에 얼마나 기여했는가.

| 점수 | 기준 |
|------|------|
| 90~100 | RAG 경험이 판단의 핵심 근거. 경험 없이는 다른 결정을 했을 것 |
| 70~89 | RAG 경험이 판단에 유의미하게 반영. 확신도나 비중에 영향 |
| 50~69 | RAG 경험이 언급됐으나 판단에 결정적이지 않음 |
| 30~49 | RAG 경험이 거의 무시됨. 파이프라인 결론에만 의존 |
| 0~29 | RAG 경험이 오히려 판단을 방해하거나 완전히 무관 |

### 2-4. 프롬프트 추가

기존 회고분석 프롬프트 출력 형식에 추가:

```
**Output Format**:
- Reflection: 포괄적 분석 (800-1200 토큰)
- Key Lessons: RAG 쿼리용 간결한 요약 (200-400 토큰)
- analysis_accuracy: 정수 (0~100)
- rag_contribution: 정수 (0~100) 또는 null (RAG 미사용 시)
```

### 2-5. 파싱

```python
import re

def _parse_scores(text: str) -> tuple[int | None, int | None]:
    accuracy = None
    contribution = None

    m = re.search(r'analysis_accuracy\s*[:=]\s*(\d+)', text)
    if m:
        accuracy = max(0, min(100, int(m.group(1))))

    m = re.search(r'rag_contribution\s*[:=]\s*(\d+|null)', text, re.IGNORECASE)
    if m and m.group(1).lower() != 'null':
        contribution = max(0, min(100, int(m.group(1))))

    return accuracy, contribution
```

파싱 실패 시 NULL 저장. 회고분석 결과 자체는 정상 저장.

### 2-6. 대시보드 표시

```
분석 정확도: 72점 (23건 평균)
RAG 기여도: 58점 (RAG 사용 15건 평균)
```

`GET /metrics` 응답에 추가:

```json
{
  "analysis_accuracy_avg": 72.3,
  "analysis_accuracy_count": 23,
  "rag_contribution_avg": 58.1,
  "rag_contribution_count": 15
}
```

---

## 3. 포트폴리오 모드 [토론 진행 중]

### 3-0. 결정된 사항

#### 분석검증모드 PA는 변경하지 않는다

분석검증모드는 통제된 실험이다. 변수를 최소화하여 분석 품질을 순수 측정한다.

```
12에이전트 분석 → 고정 결론 → PA(실행자) + 유일한 변수 RAG → 결과
```

PA에 전략 자유도를 주면 "분석이 맞았는지 PA가 잘한 건지" 귀인이 불가능해진다. 분석검증 PA는 실행자로 유지.

#### 포트폴리오 모드는 기존 분석 루프와 완전히 분리된 별도 진입 포인트

기존 `분석 → PA → 요약 → 반성` 루프 안에 포트폴리오 PA를 넣지 않는다.

```
[기존 루프 — 분석검증모드, 변경 없음]
스케줄 트리거 → NVDA 분석+PA+요약 → TSLA 분석+PA+요약 → ... → 전부 완료

[포트폴리오 모드 — 별도 진입]
오늘치 전체 스케줄 완료 감지 → 포트폴리오 큐잉 → 비서 → 포트폴리오 PA → 매매 → (청산 시) 반성
```

이유: 포트폴리오 PA는 **전체 티커의 분석 결과를 한꺼번에** 봐야 전략을 짤 수 있다. 종목별 순차 루프 안에 넣으면 "NVDA 판단 시 TSLA 분석이 아직 안 끝남" → 전체 비교 불가 → 포트폴리오 전략 수립 불가.

#### 포트폴리오 모드는 활성화/비활성화 개념

환경변수 또는 설정으로 on/off. 활성화 시 오늘치 스케줄이 전부 완료되면 자동으로 포트폴리오 큐잉.

#### 12에이전트 파이프라인은 양 모드에서 동일

분석은 객관적으로 동일하게 수행된다. 모드 차이는 PA 이후에만 발생.

#### 포트폴리오 PA는 별도 클래스 (PortfolioManagerAgent)

프롬프트 분기가 아닌 별도 클래스로 분리. 입력 데이터, 판단 로직, 출력 포맷이 전부 다르기 때문.

| 클래스 | 역할 |
|--------|------|
| `PortfolioAgent` (기존) | 분석검증 PA — 실행자 |
| `PortfolioManagerAgent` (신규) | 포트폴리오 PA — 펀드매니저 |

#### 포트폴리오 PA 앞에 비서 에이전트 배치

21개 티커의 요약본을 PA에 전부 넣으면 컨텍스트가 과도하다. 비서가 압축한다.

비서 입력: 각 티커의 **시장 분석(market_report) + 최종 결정(final_trade_decision)** + 현재 포트폴리오 상태

비서 출력: 오늘 시장 전체 흐름 + 종목별 핵심 신호 + 포트폴리오 현황 요약

비서의 출력이 곧 **포트폴리오 RAG 쿼리**로도 사용된다. "테크 섹터 강세, BUY 신호 다수, 현재 테크 편중 35%" → 과거 비슷한 상황에서의 배분 경험 검색.

#### 포트폴리오 PA는 요약하지 않는다

포트폴리오 PA는 요약된 reports를 받아서 읽는 소비자. 자기 산출물을 요약에이전트에 태울 필요 없음.

#### 반성 테이블은 기존과 별도

분석검증 반성은 "이 종목의 분석이 맞았는가", 포트폴리오 반성은 "이 배분이 적절했는가". 교훈의 성격이 다르므로 테이블 분리. ChromaDB 컬렉션도 모드별 분리.

#### RAG는 교차 참조하지 않는다 (분석검증 ↔ 포트폴리오)

분석검증 PA가 포트폴리오 반성을 보면 변수 통제가 깨진다. "포트폴리오 편중 때문에 줄여라"는 경험이 분석검증 PA에 주입되면 분석 정확도 측정이 오염.

포트폴리오 PA가 분석검증 반성을 보는 것은 가능 — "RSI 70에서 진입하면 손실"은 포트폴리오 PA에게도 유효한 종목 레벨 교훈. **단, 이건 미결정 사항으로 추후 판단.**

#### 자동 청산 없음 (포트폴리오 모드)

포트폴리오 PA가 펀드매니저 역할이므로, ±30% 자동 청산 없이 PA 판단에 맡기는 것이 일관적. 분석검증모드의 자동 청산(FR-042)은 그대로 유지.

### 3-1. 미결정 사항

#### (1) 포트폴리오 반성 트리거

**청산 시에만** 반성할 것인가, **드로다운 기반**으로도 트리거할 것인가.

- 청산 시에만: 결과가 확정된 뒤에만 반성 → 퀄리티 최고. 단, 리밸런싱만 하면서 청산이 안 나는 기간에는 반성 데이터가 안 쌓임.
- 드로다운 기반 추가: 포트폴리오 전체 수익률이 고점 대비 N% 하락하면 반성 트리거. 청산 없이도 "왜 빠졌는가" 분석 가능. 실제 펀드매니저도 큰 손실 시 반성함.

```
후보 A: 청산 시에만 → RAG 저장
후보 B: 청산 시 + 드로다운 N% 초과 시 → 둘 다 RAG 저장
후보 C: 청산 시(RAG 저장) + 주간 정기(기록만, RAG 안 태움)
```

현재 기울어진 방향: **후보 A (청산 시에만)**. 반성이 안 쌓이는 문제가 발생하면 그때 드로다운 기반 추가.

#### (2) 포트폴리오 PA가 분석검증 반성(RAG)을 참조할 수 있는가

종목 레벨 교훈("RSI 70에서 진입하면 손실")은 포트폴리오 PA에게도 유효하다. 그러나 두 모드의 RAG를 연결하면 시스템 복잡도가 증가하고, 교차 오염 가능성이 생긴다.

```
후보 A: 완전 분리 — 포트폴리오 PA는 포트폴리오 반성만 참조
후보 B: 단방향 참조 — 포트폴리오 PA는 분석검증 반성도 읽을 수 있음 (역방향은 차단)
```

미결정. 데이터가 쌓인 후 판단.

#### (3) 포트폴리오 모드의 스키마 상세

mode 컬럼 추가 방향은 결정됐으나, 구체적 DDL/제약/마이그레이션은 미정의.

- `schedule_configs`: mode 컬럼 + UNIQUE (ticker, mode)
- `positions` / `trades`: mode 컬럼
- `portfolio_configs` 테이블 또는 환경변수
- 포트폴리오 전용 반성 테이블 (이름/스키마 미정)

#### (4) 비서 에이전트 상세

- 정식 명칭 미정
- LLM 모델 (quick_think / deep_think) 미정
- 출력 포맷 미정
- 비서 출력을 RAG 쿼리로 사용하는 구체적 방식 미정

#### (5) 포트폴리오 모드의 우선순위

포트폴리오 모드는 분석검증이 충분히 검증된 뒤에 착수한다. 회고분석 배점은 먼저 구현 가능.

현재 분석검증모드의 승률이 55% 이상 확인되면, 균등 배분 방식(총자산/N)으로 소액 실전 테스트가 먼저이고, 포트폴리오 PA는 그 이후 단계.

```
구현 순서:
1. 회고분석 배점 [즉시 가능]
2. 분석검증 승률 확인 [데이터 축적 대기]
3. 균등 배분 실전 테스트 [승률 확인 후]
4. 포트폴리오 모드 구현 [3번 이후]
```

---

## 4. 결정 사항 요약

| 항목 | 결정 | 상태 |
|------|------|------|
| 회고분석 배점 (`analysis_accuracy`, `rag_contribution`) | 0~100 정수, retrospective_analyses 컬럼 추가 | ✅ 결정 |
| 배점 기준 (루브릭) | 5단계 (0~29, 30~49, 50~69, 70~89, 90~100) | ✅ 결정 |
| 대시보드 표시 | metrics API에 평균값 추가 | ✅ 결정 |
| 분석검증 PA 유지 | 실행자 역할 변경 없음 | ✅ 결정 |
| 포트폴리오 모드 별도 진입 포인트 | 기존 루프 밖, 전체 스케줄 완료 후 실행 | ✅ 결정 |
| 포트폴리오 PA 별도 클래스 | `PortfolioManagerAgent` | ✅ 결정 |
| 비서 에이전트 배치 | 포트폴리오 PA 앞단, 입력: market_report + final_decision | ✅ 결정 |
| 비서 출력 = RAG 쿼리 | 비서 요약이 곧 포트폴리오 RAG 검색 쿼리 | ✅ 결정 |
| 포트폴리오 PA 요약 안 함 | 요약에이전트 미사용, 반성만 수행 | ✅ 결정 |
| 반성 테이블 분리 | 포트폴리오 전용 반성 테이블 + ChromaDB 컬렉션 별도 | ✅ 결정 |
| 분석검증 ↔ 포트폴리오 RAG 교차 참조 차단 (분석검증 방향) | 분석검증 PA는 포트폴리오 반성을 보지 않음 | ✅ 결정 |
| 포트폴리오 자동 청산 없음 | PA 판단에 위임 | ✅ 결정 |
| 12에이전트 파이프라인 양 모드 동일 | 분석은 동일, PA 이후만 분기 | ✅ 결정 |
| 포트폴리오 반성 트리거 | 청산 시에만 vs 드로다운 추가 | ⬜ 미결정 |
| 포트폴리오 PA → 분석검증 반성 참조 가능 여부 | 단방향 허용 vs 완전 분리 | ⬜ 미결정 |
| 포트폴리오 스키마 상세 (DDL, 제약) | 방향만 결정, 상세 미정 | ⬜ 미결정 |
| 비서 에이전트 상세 (명칭, 모델, 출력 포맷) | 역할만 결정, 상세 미정 | ⬜ 미결정 |
| 포트폴리오 모드 구현 시점 | 분석검증 승률 확인 → 균등 배분 테스트 → 그 이후 | ⬜ 미결정 |
